import { fireEvent, render } from "@testing-library/react";
import Router from "next/router";
import { HomeContentCard } from "./card";

describe("home content card", () => {
    it.todo("redirects to the url when clicked");
});
